# Full name shown by `security find-identity -v -p codesigning`.
SIGNING_IDENTITY='Developer ID Application: Example Developer (ABCDE12345)'

# Local Keychain profile created by `xcrun notarytool store-credentials`.
NOTARY_PROFILE='macos-notarization'
